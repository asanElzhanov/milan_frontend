import { PlaceholderPage } from '../../_components/placeholder-page';

export default function OrdersPlaceholderPage() {
  return (
    <PlaceholderPage
      title="Заказы"
      description="История заказов будет реализована после Auth integration и Orders API layer."
      note="Раздел пока не вызывает API и не содержит бизнес-логики."
    />
  );
}
