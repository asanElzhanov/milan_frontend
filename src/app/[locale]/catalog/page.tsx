import { PlaceholderPage } from '../_components/placeholder-page';

export default function CatalogPage() {
  return (
    <PlaceholderPage
      title="Каталог"
      description="Здесь появятся категории, фильтры, сортировка и карточки товаров."
      note="Каталог будет подключен отдельным prompt без mockData и без временной бизнес-логики."
    />
  );
}
