import { PlaceholderPage } from '../_components/placeholder-page';

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Вход и регистрация"
      description="Авторизация будет добавлена отдельно после базовых layout widgets."
      note="Форма входа пока не создается, чтобы не смешивать shell и auth-flow."
    />
  );
}
