# Order Entity

`src/entities/order` currently exposes checkout-oriented order result types and adapters.

Included:

- `CheckoutOrder`
- `CheckoutResult`
- `adaptCheckoutOrder`
- `adaptCheckoutResult`

Full order history, order detail pages, and payment APIs are intentionally outside this entity step.
